---
/**
 * Process.astro
 * Step-by-step workflow visualization for the Seed & Source journey.
 */

interface Step {
  id: string;
  title: string;
  description: string;
  icon: string;
}

interface Props {
  title?: string;
  steps?: Step[];
}

const {
  title = "Our Germination Process",
  steps = [
    {
      id: "01",
      title: "Seed Generation",
      description: "Initialize your project via the SSCLI with your preferred templates and architecture.",
      icon: "🌱"
    },
    {
      id: "02",
      title: "Root Configuration",
      description: "Customize your hex-core entities and interfaces. We provide the soil, you plant the logic.",
      icon: "🌿"
    },
    {
      id: "03",
      title: "Orchestrated Growth",
      description: "Automatic wiring of your services via Docker and Terraform. Scale from one to millions.",
      icon: "🌳"
    }
  ]
} = Astro.props;
---

{/* [SS-FEATURE-PROCESS-START] */}
<section class="py-24 bg-foundry-surface/30">
  <div class="max-w-7xl mx-auto px-4">
    <h2 class="text-3xl font-bold text-center text-foundry-text mb-20">{title}</h2>
    
    <div class="relative">
      {/* Connector line for desktop */}
      <div class="hidden md:block absolute top-[2.75rem] left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-foundry-secondary/20 via-foundry-secondary to-foundry-secondary/20"></div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-16 relative z-10">
        {steps.map((step) => (
          <div class="text-center group">
            <div class="w-20 h-20 mx-auto rounded-full bg-white shadow-lg border-2 border-foundry-secondary flex items-center justify-center text-3xl mb-8 group-hover:scale-110 transition-transform duration-500">
              {step.icon}
            </div>
            <div class="inline-block px-3 py-1 rounded bg-foundry-secondary/10 text-foundry-secondary font-mono text-sm mb-4">
              STEP {step.id}
            </div>
            <h3 class="text-xl font-bold text-foundry-text mb-4">{step.title}</h3>
            <p class="text-foundry-text/70 leading-relaxed max-w-sm mx-auto">{step.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
{/* [SS-FEATURE-PROCESS-END] */}
