---
/**
 * Integrations.astro
 * Grid or scrolling logo cloud for Seed & Source supported tools.
 */

interface Integration {
  name: string;
  icon: string;
}

interface Props {
  title?: string;
  integrations?: Integration[];
}

const {
  title = "Plays Well With Others",
  integrations = [
    { name: "GitHub", icon: "🌐" },
    { name: "Terraform", icon: "💎" },
    { name: "Docker", icon: "🐳" },
    { name: "Astro", icon: "🚀" },
    { name: "Tailwind", icon: "🎨" },
    { name: "Vitest", icon: "🧪" }
  ]
} = Astro.props;
---

{/* [SS-FEATURE-INTEGRATIONS-START] */}
<section class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4">
    <p class="text-center text-foundry-text/50 font-medium uppercase tracking-widest text-sm mb-12">{title}</p>
    
    <div class="flex flex-wrap justify-center gap-10 md:gap-20 opacity-40 grayscale hover:grayscale-0 transition-all duration-700">
      {integrations.map((item) => (
        <div class="flex items-center gap-3 group">
          <span class="text-3xl filter transition-all duration-300 group-hover:drop-shadow-glow">{item.icon}</span>
          <span class="text-xl font-bold text-foundry-text hidden sm:inline">{item.name}</span>
        </div>
      ))}
    </div>
  </div>
</section>
{/* [SS-FEATURE-INTEGRATIONS-END] */}
